import LinearProgress from '@mui/material/LinearProgress';

/**
 * Progress bar component. Centralizes @mui/material LinearProgress usage.
 */
const ProgressBar = LinearProgress;

export default ProgressBar;
