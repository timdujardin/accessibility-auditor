import MuiFormControl from '@mui/material/FormControl';
import MuiFormHelperText from '@mui/material/FormHelperText';
import MuiInputLabel from '@mui/material/InputLabel';
import MuiMenuItem from '@mui/material/MenuItem';
import MuiSelect from '@mui/material/Select';

/**
 * SelectInput components. Centralizes @mui/material Select-related usage.
 */
const SelectInput = MuiSelect;
const SelectFormControl = MuiFormControl;
const SelectFormHelperText = MuiFormHelperText;
const SelectLabel = MuiInputLabel;
const SelectOption = MuiMenuItem;

export { SelectInput, SelectFormControl, SelectFormHelperText, SelectLabel, SelectOption };
