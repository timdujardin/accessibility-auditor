import CircularProgress from '@mui/material/CircularProgress';

/**
 * Spinner component. Centralizes @mui/material CircularProgress usage.
 */
const Spinner = CircularProgress;

export default Spinner;
