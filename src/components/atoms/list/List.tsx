import MuiList from '@mui/material/List';
import MuiListItem from '@mui/material/ListItem';
import MuiListItemText from '@mui/material/ListItemText';

/**
 * List component. Centralizes @mui/material List usage.
 */
const List = MuiList;

/**
 * List item component. Centralizes @mui/material ListItem usage.
 */
const ListItem = MuiListItem;

/**
 * List item text component. Centralizes @mui/material ListItemText usage.
 */
const ListItemText = MuiListItemText;

export { List, ListItem, ListItemText };
